name: wallet_project
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  reown_appkit: ^1.7.7
  dartx: ^1.2.0
  dash_chat_2: ^0.0.21
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
dependency_overrides: 
  intl: ^0.19.0

flutter:
  uses-material-design: true
